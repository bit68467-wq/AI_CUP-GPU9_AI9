services:
 - type: web
   name: backend-app
   runtime: node
   plan: free
   buildCommand: npm install
   startCommand: npm start
   envVars:
    - key: DATABASE_URL
      fromDatabase:
        name: backend-db
        property: connectionString

databases:
 - name: backend-db
   plan: free
